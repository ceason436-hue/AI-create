services:
  next-app:
    build:
      context: ./ai-music-app
      dockerfile: Dockerfile
    container_name: ai-music-app
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
