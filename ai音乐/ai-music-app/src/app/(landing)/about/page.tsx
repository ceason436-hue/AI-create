import { PublicPage, PlaceholderImage } from "@/components/public-page";
import { getPublicCampuses, getPublicTeachers } from "@/lib/public-content";

export default async function AboutPage() { const [teachers, campuses] = await Promise.all([getPublicTeachers(), getPublicCampuses()]); return <PublicPage eyebrow="ABOUT KRT AI" title="我们相信，科技教育应该从动手开始" intro="科瑞特 AI 将人工智能、机器人和创作任务放进孩子能理解的学习路径里。"><section className="public-content about-grid"><div><PlaceholderImage src="/haibao1.png" alt="科瑞特 AI 品牌资料占位图" /><h2>把复杂的工具，变成清晰的创作过程</h2><p className="lead-copy">孩子不需要先成为专家，才有资格开始创造。我们把每个工具拆成可观察、可尝试、可复盘的步骤，让作品成为学习发生过的证据。</p></div><div className="about-panels"><div><span className="eyebrow dark">TEACHERS</span><h2>师资团队</h2>{teachers.map((teacher) => <p key={teacher.id}><strong>{teacher.title}</strong>{teacher.summary}</p>)}</div><div><span className="eyebrow dark">CAMPUSES</span><h2>校区与环境</h2>{campuses.map((campus) => <p key={campus.id}><strong>{campus.title}</strong>{campus.summary}</p>)}</div></div></section></PublicPage>; }
