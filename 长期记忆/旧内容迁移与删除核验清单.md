# 科瑞特 AI 旧内容迁移与删除核验清单

> 核验日期：2026-09-02（源码与哈希于本轮复核）
> 适用仓库：`https://github.com/ceason436-hue/AI-create`  
> 规则：本清单只记录核验结果和候选范围，不代表已授权删除。

## 1. 核验范围

已扫描项目源码、正式应用源码和长期记忆文档中的旧内容文件名、引用路径、图片引用和重定向配置。当前正式应用入口为 `ai音乐/ai-music-app`；旧静态文件不作为正式应用入口。

| 文件 | 当前判断 | 引用扫描结果 | 处理建议 |
|---|---|---|---|
| `code.html` | `[REMOVE-CANDIDATE]` 旧版不完整静态首页 | 正式应用源码无运行时引用；文件自身仍引用根目录 `logo2.png`、`haibao1.png`、`tu1.jpg`、`tu2.png`，并包含多个 `#` 占位链接 | 保留至主站浏览器回归、功能对照和负责人确认完成；确认后可删除或归档 |
| `minimax_docs.html` | `[REMOVE-CANDIDATE]` 外部 MiniMax 文档快照 | 正式应用源码无引用；文件内部是独立 `/docs` 文档快照并引用外部 MiniMax 资源 | 先保留，确认无本地文档查阅需求后删除或移入归档 |
| `minimax_t2i.html` | `[REMOVE-CANDIDATE]` 外部 MiniMax 文生图文档快照 | 正式应用源码无引用；文件内部是独立 `/docs` 文档快照并引用外部 MiniMax 资源 | 先保留，确认无 API 排查需求后删除或移入归档 |

## 2. 根目录图片核验

根目录图片与正式应用 `public/` 中存在同名副本，当前正式应用已通过 `public/` 路径使用它们。不得在未确认前删除任一份。

| 资源 | 正式应用使用 | 当前用途 |
|---|---|---|
| `logo2.png` | `/logo2.png` | 主站、工具壳层、登录注册和页脚 Logo |
| `haibao1.png` | `/haibao1.png` | 主站、关于页概念/占位图 |
| `tu1.jpg` | `/tu1.jpg` | 首页场景、工具卡、课程/活动占位图 |
| `tu2.png` | `/tu2.png` | AI 作品卡、成果占位图 |

本轮 SHA256 核验结果（根目录与 `ai音乐/ai-music-app/public/` 的同名文件逐一一致）：

| 资源 | SHA256 |
|---|---|
| `logo2.png` | `756BBE82C2EE773A0B18FECB1F1B260EF7B1BD698D482E72D2420A10B535EAFB` |
| `haibao1.png` | `2440A63C2B07232D586A6A4552EC0015FD50EDD7874E8F2B744BB201CA24C73B` |
| `tu1.jpg` | `CD68D1A01F05E5D2C20D02C73A7FE451A8D5997681D9FB5F6289F735748665BA` |
| `tu2.png` | `8A20D864835E8FCF4CA658FA9DE5B84FA14E23DA5812BB5137654B06A8B2A288` |

## 3. 已建立的迁移保护

- 新主导航已经指向 `/courses`、`/school-cooperation`、`/activities`、`/achievements`、`/about`、`/tools`、`/login` 和 `/consult`。
- 已建立旧工具地址到新工具目录的重定向：`/ai-music`、`/ai-art`、`/ai-programming`、`/ai-reading`、`/ai-reading/workspace`、`/ai-reading/overview`、`/ai-music/creation`、`/stage1/*`。
- 已用真实浏览器验证 `/ai-art` 会到达 `/tools/ai-art`；课程、工具、咨询和移动端折叠导航已完成冒烟验证。
- 2026-09-02 已在真实本地开发服务逐条请求 12 个旧工具地址：`/ai-music`、`/ai-art`、`/ai-programming`、`/ai-reading`、`/ai-reading/workspace`、`/ai-reading/overview`、`/ai-music/creation`、`/stage1/rhythm`、`/stage1/pitch-explorer`、`/stage1/melody`、`/stage1/three-keys` 和 `/stage1`，均返回 `308` 且 `Location` 与规划中的新工具地址一致。自动化回归测试同时覆盖这些规则。
- 2026-09-02 已在真实本地开发服务验证历史培训注册入口 `/register/training` 返回 `307 → /register`；源码使用服务端 `redirect("/register")`，公开导航也仅指向统一注册页。该迁移已满足保留旧入口一轮的要求，暂不删除页面文件。
- 未执行旧文件、根目录图片、手册原文件和其他重复资源的删除。

## 4. 删除前置条件

只有在以下条件全部满足后，才可形成最终删除清单并请求负责人确认：

1. 完成生产构建和部署环境 URL 回归。
2. 对所有旧文件做完整引用扫描、资源哈希核对和功能对照。
3. 确认 MiniMax 文档快照不再承担离线排错或资料留档用途。
4. 确认根目录图片没有被外部脚本、文档或人工流程引用。
5. 确认所有旧 URL 有重定向、归档页或明确下线说明。
6. 负责人书面确认实际删除或归档范围。
