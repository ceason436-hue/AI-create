services:
  next-app:
    build:
      context: ./ai-music-app
      dockerfile: Dockerfile
    container_name: ai-music-app
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    env_file:
      - ./ai-music-app/.env.production.local
    environment:
      - NODE_ENV=production
