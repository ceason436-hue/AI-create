# 新对话完整交接提示词：科瑞特 AI 生产化对接

将下面整段内容复制到一个新的 Codex 对话中。新对话必须在同一项目目录运行。

```text
你现在接手“科瑞特 AI 网站生产化前后端对接与全功能验收”任务。你是本项目的总技术负责人，负责架构决策、任务拆分、子 agent 协作、代码集成、质量门禁、风险控制和最终验收。

本任务采用强制的“资料收集门禁”。第一阶段只能只读检查代码、文档和当前环境，先告诉我完成任务需要我提供什么、每项怎样安全提供、缺少后会阻塞什么。任何不清楚的问题都必须优先向我提出并等待确认，不得猜测后继续。只有我明确回复“资料已齐，可以开工”后，你才能修改代码、执行数据库迁移、创建测试数据或操作云资源。收到该确认后，不要停在方案或审计阶段；请持续实施、测试和修复，直到所有已确认范围内的工作完成。

项目根目录：
D:\programe\AI科瑞特\AI-create

正式 Next.js 应用目录：
D:\programe\AI科瑞特\AI-create

开始前必须依次阅读：
1. D:\programe\AI科瑞特\AI-create\AGENTS.md
2. D:\programe\AI科瑞特\AI-create\长期记忆\科瑞特AI生产化前后端对接与全功能验收基线.md
3. D:\programe\AI科瑞特\AI-create\长期记忆\生产资源提供清单与安全交接模板.md
4. D:\programe\AI科瑞特\AI-create\长期记忆\归档\2026-09-前端视觉整改\归档索引.md
5. D:\programe\AI科瑞特\AI-create\长期记忆\归档\2026-09-前端视觉整改\生产化历史参考\科瑞特AI平台用户分级与商业化规划.md
6. 应用的 prisma/schema.prisma、package.json、.env.example、.env.platform.example，以及 src/lib/ai-gateway.ts、src/lib/auth.ts、src/lib/storage.ts、src/app/api/works/route.ts。

当前事实（必须重新用代码和命令验证，不要盲信旧文档）：
- 2026-09-05 本机 PostgreSQL 16 和 Redis 7 为 healthy，Prisma 9 项迁移同步。
- 19 个 Vitest 文件、39 项测试通过，凭据扫描通过。
- 站内实际可达的 51 个链接在未登录状态下全部返回 200，历史别名正常重定向。
- 数据库审计时有 4 个账号、1 个启用管理员、6 个启用 AI 工具；课程、作品、咨询、媒体、课件均为空。
- 最新视觉整改后的页面大量使用 src/components/reference-*.tsx。视觉必须保留，不能退回旧版。
- 最新视觉组件没有完整读取 CMS、课程、活动、成果、媒体位和 AiTool 数据，后台发布与前台展示脱节。
- ReferenceProgramming 是固定字符串检查和固定迷宫结果，没有调用 /api/minimax/code，历史只在 localStorage。
- ReferenceReadingImport 只等待 350ms 跳转；ReferenceReadingStory 永远显示固定“小蚂蚁搬苹果”，不读取用户文章；DOCX 被 readAsText 错误处理。
- ReferenceMusic 会调用 AI，但曲目和收藏只保存在 localStorage，没有创建 MUSIC 云端作品。
- ReferenceArt 会调用 AI 并尽力保存云端，但历史仍以 localStorage 为主，存在双状态和静默失败。
- /my-works 同时读取服务端作品和本地旧绘画记录。
- 项目负责人已购买阿里云 RDS，并已创建 OSS Bucket；这只表示资源存在，连接参数、网络、权限、Secret 注入、迁移和真实读写仍需收集与验证。
- 生产 Redis、LibreOffice、FFmpeg、Worker Token 尚未确认或就绪；AI 生产开关应默认 false。
- 当前 MiniMax 图生图字段仍有未验证的推测性注释，必须按当前官方契约和真实预发布响应确认。

不可退让的目标：
1. 保留现有已完成的全站视觉、响应式和动效。改数据层时不要大面积重写 CSS 或替换现有图片。
2. 公开站、学习中心和运营后台必须使用同一真实数据源。后台发布、下线或修改内容后，前台应即时按缓存策略反映。
3. 四个 AI 工具都必须执行真实声明的功能。禁止固定输出、假进度、假成功、假控制台和与用户输入无关的示例结果冒充生产结果。
4. 个人/培训账号的作品进入 PostgreSQL + 私有对象存储；学校共享账号只使用 12 小时当堂 sessionStorage；匿名试用不写个人作品。
5. 权限、工具开关、报名、点数、并发、限流、幂等和课程上下文都由服务端强制执行，不能靠隐藏按钮。
6. 前端不能直接依赖 MiniMax 原始响应。先建立统一的站内 AI 请求、成功和错误契约，再让各工具消费。
7. 不要在源码、文档、输出、提交或日志中暴露真实 Secret、密码、学生资料、提示词全文或 Base64 内容。
8. 不允许为了让测试通过而关闭鉴权、限流、审计、点数或存储校验。

阶段 0：资料收集门禁（收到“资料已齐，可以开工”前只执行本阶段）
- 只读检查 git 状态、AGENTS.md、主基线、资源模板、环境变量名、部署脚本和现有接口；不得编辑文件、迁移数据、创建账号、调用付费 AI 或改变云资源。
- 第一条正式回复必须包含：已知资源、必须提供项、可选项、每项安全提供方式、验证方式、缺失影响，以及所有需要我决定的问题。
- 使用《生产资源提供清单与安全交接模板》的字段逐项收集 RDS、Redis、OSS、ECS/部署、域名、MiniMax、Worker、测试身份、授权素材、业务规则、发布范围、备份和监控信息。
- 不要求我把密码、完整连接串、AccessKey、API Key、Token、管理员密码或 SSH 私钥发到对话中。指导我把 `.env.platform.example` 复制为 gitignored 的 `.env.production.local`，或把同名变量写入部署平台 Secret。你只检查变量是否存在和连通性结果，任何输出都必须脱敏。
- 汇总状态时使用 `已收到 / 已写入 Secret / 待提供 / 不适用 / 已验证`，并明确阻塞影响。信息有矛盾、含义不清、存在多个会影响安全/成本/数据/产品行为的方案时，先问我并等待确认。
- 我可以分多次提供资料。每次收到后更新缺项表，不得因为“多数资料已齐”提前开工。
- 只有我明确发送“资料已齐，可以开工”后，才进入阶段 A。

总技术负责人和子 agent 协作规则：
- 上述“遇到不清楚的问题先问我并等待确认”适用于整个任务，不只适用于阶段 0。即使已经开工，后续发现新的需求歧义、生产数据风险、费用选择、安全边界或发布范围，也要暂停相关分支并先问我；其他已确认且互不依赖的工作可以继续。
- 开工后根据任务依赖把可独立并行的工作分配给子 agent，例如：架构与 API 契约、AI 工具去 Mock、认证/运营后台/数据闭环、E2E/部署与安全验证。
- 给每个子 agent 明确文件边界、输入、完成定义和验证命令，避免多人同时修改同一文件。总技术负责人负责依赖排序、冲突处理、集成测试和最终结论。
- 子 agent 的报告只能作为证据，不能替代总技术负责人的代码复核和端到端验收；涉及生产数据、费用、安全、公开发布或需求不清的问题，由总技术负责人集中向我提问并等待确认。
- 优先并行只读审计和互不冲突的实现；数据库迁移、共享配置、生产操作和最终集成必须按依赖顺序执行。

按以下顺序实施：

阶段 A：建立回归基线
- 检查 git status，区分已有视觉改动和本次生产化改动，不覆盖用户现有工作。
- 运行 npm test、npm run lint、npm run security:scan、npm run build。
- 枚举所有 page.tsx 和 route.ts，建立可机器执行的路由/API 清单。
- 新增 Playwright 或等价 E2E 基线，至少覆盖首页、课程、工具、登录、注册、咨询、学习中心、作品中心和后台鉴权。

阶段 B：让最新视觉页面重新读取真实后台数据
- 把 reference-* 组件改成接收明确类型的 props；服务端页面读取 SitePage/PageSection、MediaSlot、Course、Activity、Achievement、Teacher、Campus、Partner、AiTool。
- 保留当前视觉结构与动效；只替换数据来源和空/错/加载状态。
- 增加公开 AI 工具目录读取契约，只返回 ACTIVE 且 visibleToPublic 的白名单字段。
- 处理静态课程方向 Slug 与数据库可报名课程 Slug 的冲突，定义稳定 URL 规则并补重定向测试。
- 后台文案必须与真实能力一致；若某视觉区块仍不受后台管理，就不要声称“发布后立即生效”。

阶段 C：统一 AI 服务层
- 为 image、music、code、reading 定义稳定站内请求/响应/错误码；所有请求带 Idempotency-Key。
- 保留 src/lib/ai-gateway.ts 的身份、试用、工具开关、Redis 限流、并发、课程绑定、点数预占/结算/释放与审计。
- 供应商适配层负责 MiniMax 原始字段，前端只读取站内契约。
- 对取消、超时、供应商 4xx/5xx、非法 JSON、空结果、重复请求和存储失败写集成测试。

阶段 D：逐工具去 Mock
- 绘画：真实文生图/图生图；服务端校验上传；生成结果可靠创建 IMAGE 作品；云端保存失败可重试，不重复扣点；历史只读作品 API。
- 音乐：处理同步/异步结果；服务端把音频复制到私有 OSS 并创建 MUSIC 作品；历史、收藏、下载、删除和继续编辑基于真实数据。
- 编程：当前 ReferenceProgramming 必须接 /api/minimax/code；删除固定成功路线和伪控制台。HTML 在严格 sandbox iframe 预览。Scratch/Python/C++ 只有在存在真实执行器时才显示“运行成功”，否则如实提供生成/静态检查/下载。
- 阅读：真实解析 TXT/MD/DOCX；真实分段、结构、关键词、问题和证据；结果必须来自用户文章；逐段问答携带正确上下文；绘本图与段落关联；保存为 READING JSON 作品。
- /my-works 统一云端作品；仅提供一次明确的旧 localStorage 迁移，迁移成功后清理对应记录。

阶段 E：认证、后台和学习闭环
- 使用自动创建并最终清理的临时测试数据，验证社会注册、培训邀请码注册、个人登录、学校登录、退出、停用和会话过期。
- 验证课程分类→课程→模块→课时→公告→工具绑定→报名→学习→进度→课程作品完整闭环。
- 验证后台学校、培训、邀请码、套餐、订单、点数、AI 设置、内容、媒体、页面区块和审计。
- 不使用真实学生资料。若需要已有管理员密码或生产 Secret，先完成所有不依赖它们的代码和自动化，再只列出最小外部输入。

阶段 F：生产基础设施与验收
- 使用 .env.platform.example 逐项预检，不输出变量值。
- 生产使用 RDS/受控 PostgreSQL、Redis 和私有 OSS；应用端口仅供 Nginx/本机代理访问。
- 安装并验证 LibreOffice、FFmpeg；配置课件/媒体 Worker 和 npm run ai:recover、npm run works:retention 定时任务。
- 用授权的 PPT/PPTX/DOC/DOCX/PDF/图片/MP4/WebM/WebVTT 验证课件和媒体链路。
- 完成匿名、个人、培训、学校、管理员五类身份测试；完成 30/60 并发、成本、失败恢复和全局停用测试。

每完成一个阶段都必须：
- 更新 D:\programe\AI科瑞特\AI-create\长期记忆\科瑞特AI生产化前后端对接与全功能验收基线.md 的真实状态。
- 运行与改动相关的测试，修复回归后再继续。
- 汇报具体改动、验证证据、剩余风险和确实需要外部提供的内容。
- 不要把“代码存在”“页面能打开”写成“功能已完成”；只有真实数据与角色端到端通过才标记已验证。

最终交付必须包含：
- 可运行的生产化代码，不含 Mock 主链路。
- 前后端接口契约及生成的类型/校验。
- 角色和功能 E2E、API 集成测试、单元测试。
- 数据迁移和旧 localStorage 迁移策略。
- 生产配置、Worker、备份、监控、灰度、回滚说明。
- 一份逐页面、逐功能的最终验收报告，附命令结果和未能验证的唯一外部阻塞。

现在只执行阶段 0：先只读盘点并给我完整的资料提交清单和待确认问题。明确告诉我如何安全提供每项数据，并等待我回复“资料已齐，可以开工”。在收到这句确认之前不得进入阶段 A。收到确认后，立即组织子 agent 按依赖并行实施，不要因为任务很大而只完成一部分。
```
