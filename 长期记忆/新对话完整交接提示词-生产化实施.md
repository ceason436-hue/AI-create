# 新对话完整交接提示词：科瑞特 AI 生产化实施

> 用法：将下面代码块中的全部内容复制到一个新的 Codex 对话。新对话必须使用同一本地项目目录。

```text
你现在接手“科瑞特 AI 网站生产化前后端对接与全功能验收”任务。你是本项目的总技术负责人，负责架构决策、任务拆分、子 agent 协作、代码集成、质量门禁、风险控制和最终验收。

重要：资料收集阶段已在上一对话完成，项目负责人已于 2026-09-06 明确发送“资料已齐，可以开工”。不要重复阶段 0，不要再要求项目负责人重新填写已确认的资源表。先完整阅读指定文档并脱敏复核环境状态，然后立即从阶段 A 开始实施。不要停在方案或审计阶段；请持续实施、测试、修复和集成，直到已确认的本地/预发布范围全部完成。

项目根目录：
D:\programe\AI科瑞特\AI-create

正式 Next.js 应用目录：
D:\programe\AI科瑞特\AI-create

开始前必须依次完整阅读：
1. D:\programe\AI科瑞特\AI-create\AGENTS.md
2. D:\programe\AI科瑞特\AI-create\长期记忆\科瑞特AI生产化资料门禁确认记录.md
3. D:\programe\AI科瑞特\AI-create\长期记忆\科瑞特AI生产化前后端对接与全功能验收基线.md
4. D:\programe\AI科瑞特\AI-create\长期记忆\生产资源提供清单与安全交接模板.md
5. D:\programe\AI科瑞特\AI-create\长期记忆\归档\2026-09-前端视觉整改\归档索引.md
6. D:\programe\AI科瑞特\AI-create\长期记忆\归档\2026-09-前端视觉整改\生产化历史参考\科瑞特AI平台用户分级与商业化规划.md
7. 应用的 prisma/schema.prisma、package.json、.env.example、.env.platform.example、src/lib/ai-gateway.ts、src/lib/auth.ts、src/lib/storage.ts、src/app/api/works/route.ts。
8. 与当前 Next.js 16.3.4 任务相关的 node_modules/next/dist/docs/ 本地文档；不能凭训练记忆假设 Next.js API。

已确认的资源、Secret 状态、业务决策、AI 预算、URL 规则、素材授权、备份监控和延期范围，以《科瑞特AI生产化资料门禁确认记录》为准。不在输出、命令、日志、测试快照或提交中回显任何 Secret。

已确认的当前实施边界：
- 先完成全部本地代码、本地 PostgreSQL/Redis/LOCAL 存储集成测试、合成验收素材和本机预发布验收。
- 生产 RDS 真实连通/迁移、OSS RAM Role 绑定与真实读写、ECS Redis、LibreOffice、FFmpeg、Worker Token、Nginx 发布、监控规则、恢复演练及 30/60 并发延后到服务器准备完后继续。
- 延期项不得写为“已完成”或“已验证”。本地范围完成时，应清楚列出唯一剩余的生产基础设施阻塞，等项目负责人提供服务器后继续阶段 F。
- 当前 2C2G ECS 不能作为 Next.js + Redis + LibreOffice + FFmpeg + 30/60 并发的最终验收环境。不自动创建或扩容付费云资源。
- 不得自动发布到正式生产、公开新内容或删除生产数据/正式 OSS 对象，这些动作仍需单独确认。

不可退让的目标：
1. 保留当前已完成的全站视觉、响应式和动效。不大面积重写 CSS，不替换已确认图片，不恢复有意删除的旧文档和压缩包。
2. 公开站、学习中心和运营后台使用同一真实数据源。后台发布、下线或修改后，前台按明确缓存策略反映。
3. 四个 AI 工具必须执行真实声明的功能。禁止固定输出、假进度、假成功、假控制台和与用户输入无关的示例结果冒充生产结果。
4. 社会/培训个人账号作品进入 PostgreSQL + 私有对象存储；学校共享账号只使用 12 小时当堂 `sessionStorage`；匿名试用不写个人作品。
5. 权限、工具开关、报名、点数、并发、限流、幂等、课程上下文和存储配额由服务端强制，不能靠隐藏按钮或可修改 Cookie。
6. 前端不直接依赖 MiniMax 原始响应。先建立稳定的站内 AI 请求、任务状态、成功结果和错误码契约，供应商字段只存在于适配层。
7. 不得为了测试通过而关闭鉴权、限流、审计、点数、幂等、课程绑定或存储校验。
8. 实施结果不等于完整法律合规认证；正式面向未成年人开放前仍需合规专项复核。

已确认的产品规则：
- 课程 URL：`/courses` 为总目录；`/courses/directions/[slug]` 为静态方向；`/courses/[courseSlug]` 为数据库真实课程。只对已知旧静态 Slug 做 308 重定向，不得吞掉数据库课程。
- AI 阅读：上传文件最大 50 MiB；解压/提取正文最大 50,000 个中文字符；只支持中文；面向一至六年级；超长内容分块后合并结构化结果；正文、分析和作品保留 30 天。
- 音乐收藏必须落库；“继续编辑”必须保留版本链。
- 编程：HTML 只在严格 sandbox iframe 中预览；Python/C++ 只生成、静态检查和下载；Scratch 没有真实执行器时不宣称执行。
- 社会用户首发开放用户名+密码注册；短信验证、找回密码和在线支付本轮不适用，套餐由管理员人工开通。
- 学校可用工具由后台逐校配置，默认全关。
- 验收所需文本、课程、DOC/DOCX/PPT/PPTX/PDF、图片、MP4/WebM/WebVTT 由代码自动生成合成素材，明确标记为测试数据，不使用真实学生资料。《AI科瑞特手册》中教师、证书、奖项、学校 Logo 和课堂照片已确认拥有公开授权。

AI 成本规则：
- `AI_GENERATION_ENABLED=false` 保持为生产安全默认。
- 本轮付费 AI 真实测试总预算 100 元，单日上限 30 元，达到总预算 80% 时停止自动测试并汇报。文本类额外上限为一亿 Token，不能代替人民币成本上限。
- 点数按 `1 点 = 0.01 元供应商成本`设计；各工具权重为单次实测成本 × 1.3 安全系数后向上取整。未实测前不猜测权重。
- 原网站 MiniMax 模型名只是候选。图生图字段、音乐同步/异步状态、模型 ID、返回结构和错误码必须依当前官方文档和脱敏真实响应验证。`MINIMAX_GROUP_ID` 已确认不适用。

开工时必须处理的已知高风险问题（必须用当前代码再验证，不盲信交接摘要）：
- 工作区有大量已有视觉改动、新文件和有意删除；不得 reset、checkout 或覆盖。先用 `git status`、diff/stat、文件清单和必要的视觉快照记录基线。
- `SitePage/PageSection`、`MediaSlot`、`Course`、`Activity`、`Achievement`、`TeacherProfile`、`Campus`、`PartnerSchool`、`AiTool` 已有数据读取底座，但最新 `reference-*` 视觉页大面积未连接。`getPublishedPageSections()`、`getPublicMediaSlots()`、`getPublicAiTools()` 等函数存在但少有或没有页面消费。
- 学校 12 小时临时存储目前只在 `(app)/layout.tsx` 的局部逻辑中尝试实现；最新 `/tools/*` 位于 `(landing)` 并且组件直接使用 `localStorage`，可能绕过该规则。必须建立统一的按真实服务端会话类型选择的存储适配层。
- `ReferenceProgramming` 没有调用代码 API，使用字符串检查、固定迷宫路线和伪控制台。`ReferenceReadingImport/Story` 使用定时跳转、固定文章与错误 DOCX 处理。必须删除冒充真实功能的逻辑。
- 音乐历史、收藏和版本链仍是 `localStorage`；绘画同时存在本地和云端双状态；`/my-works` 混合服务端作品与旧绘画记录。
- AI 网关已有身份、试用、工具开关、Redis 限流/并发、课程上下文、点数预占/结算/释放和审计，不得绕过或降级。但幂等目前只会因唯一约束返回 409，不能重放既有结果。
- AI 超时恢复与晚到响应可能竞态导致二次结算；Redis 并发释放可能重复 DECR；必须使用状态 CAS/唯一账本引用和幂等 lease 解决。
- AI 提供商成功后当前常由客户端 best-effort 保存作品；存储失败仍可能扣点且丢失成果。必须改成服务端“生成→校验/拉取→稳定存储→创建作品→结算”或可恢复的等价状态机。
- `music_query` 当前被当成独立工具、独立试用计数和独立学校权限；应改为原 music 请求所有者才能查询的子资源，不单独扣点或消耗试用次数。
- `/api/works` 目前在对象上传后才验证课程上下文，可留下孤儿对象；容量校验是读后写，并发时可超额；作品 POST 缺少幂等。
- 当前 OSS 适配器强制长期 AK/SK，不支持已确认的 ECS RAM Role；`works:retention` 在 OSS 驱动时拒绝运行；对象键尚未统一加上已确认的 `krt-ai/*` 前缀。
- 课程工具上下文对 `chat` 的特判可能在只绑定任意其他工具时也放行。阅读页的 `vision`/`image` 实际调用与课程绑定键也可能不一致。
- 后台 API 普遍已有服务端管理员校验，但 `(admin)/layout.tsx` 本身没有服务端拦截，会先渲染后台壳再等客户端 API 401。登录页的学校/个人选项也只改文案，未将预期账号类型交给服务端。
- Docker/部署已知风险：应用 Compose 的 `env_file` 路径与实际 Secret 位置不一致；Dockerfile 使用 `npm install` 而不是 `npm ci`，依赖阶段未处理 Prisma schema/generate；runner 无迁移流程、无 healthcheck；Web 容器未安装 LibreOffice/FFmpeg；当前 Worker 路由可能在 Web 容器同步处理转换。
- 当前有 19 个 Vitest 文件/39 项历史通过记录，但无 Playwright/Cypress、无 CI、无 health/readiness API。必须以当前命令重新建立基线，不把历史结果当作本轮证据。

总技术负责人与子 agent 协作规则：
- 立即根据依赖拆分可并行任务，例如：架构/AI 契约与网关、CMS/公开站/课程 URL、四工具去 Mock/作品存储、认证/学习/后台、E2E/部署/安全。
- 给每个子 agent 明确文件边界、输入、完成定义和验证命令；不让多人同时修改同一文件。
- 子 agent 报告只是证据，总技术负责人必须自己复核 diff、关键逻辑和端到端验收。
- 共享 schema、迁移、公共契约、路由和最终集成按依赖顺序执行；不让子 agent 并发修改这些共享热点。
- 遇到新的需求歧义、生产数据风险、费用选择、安全边界或公开发布决策时，暂停相关分支并向项目负责人提问；其他独立工作继续。

阶段 A：建立回归基线
- 用 `git status`、diff/stat 和文件清单区分已有视觉工作与本次生产化改动。不得使用 `git reset --hard`、`git checkout --`或其他覆盖用户改动的命令。
- 审查当前未跟踪文件、生成物和删除项，不得把 Secret、`.env*`、测试临时产物或无关文件纳入提交。
- 在应用目录运行 `npm test`、`npm run lint`、`npm run security:scan`、`npm run build`，保存当前真实结果。Next 16 build 不代替 lint。
- 枚举所有 `page.tsx` 和 `route.ts`，生成可机器执行的路由/API 清单与重复/别名检查。
- 引入 Playwright 或等价 E2E，优先对 production build/start 运行，覆盖首页、课程、工具、登录、注册、咨询、学习中心、作品中心、后台鉴权与桌面/移动关键视觉。
- 建立合成测试资料生成脚本，内容只使用虚构人物/学校与明确测试标记。

阶段 B：最新视觉页连回真实后台数据
- 保留 `reference-*` DOM/CSS/动效结构，把组件改为接收经过明确 TypeScript/Zod 类型约束的可序列化 props。
- 服务端页面或专用 loader 读取 `SitePage/PageSection`、`MediaSlot`、`Course/Category`、`Activity`、`Achievement`、`TeacherProfile`、`Campus`、`PartnerSchool`、`AiTool`。
- 明确区分“数据库为空”、“数据库故障”、“草稿不可见”和“品牌授权 fallback”；不由 `rows.length` 隐式切换一整套示例内容。
- 建立只返回 `ACTIVE + visibleToPublic` 白名单字段的公开 AI 工具目录；路由必须继续经代码白名单。
- 实施已确认的课程 URL 分层和已知旧 Slug 308 测试。
- 将数据库 `coverAssetId`、媒体位和图集通过有效 `MediaAsset` 解析为受控媒体 URL，不把资源 ID 当 URL。
- 后台发布/下线/恢复后执行对应 `revalidatePath`/tag 失效。未接入后台的区块不得宣称“发布后立即生效”。

阶段 C：统一 AI 契约、状态机和作品事务
- 为 image、music、code、reading 定义统一站内请求、任务状态、成功结果、作品引用和稳定错误码；请求必须携带 `Idempotency-Key`。
- 相同幂等键+相同请求指纹返回既有状态/结果；相同键+不同输入返回明确 409。异步任务查询只允许原请求所有者。
- 用明确状态机与 CAS 条件修复恢复/晚响应竞态、点数二次结算和 Redis 并发槽重复释放。
- 用唯一账本引用保证点数预占、成功结算、失败释放和超时恢复恰好一次。
- 供应商适配层负责 MiniMax 原始请求/响应、AbortSignal 超时、非 JSON、空结果、4xx/5xx/429、Retry-After、trace/usage 脱敏记录。
- 服务端在个人账号场景中完成供应商生成、结果校验/拉取、稳定对象存储、`Work/WorkAsset`、容量账本和点数结算。存储失败可恢复，重试不重复扣点。
- 学校/匿名强制 ephemeral，不写 `Work`或 OSS。不允许前端自行声称保存策略。
- 对作品容量、POST 幂等、课程上下文、上传补偿/孤儿清理做事务或可恢复设计。
- 为取消、超时、供应商 4xx/5xx/429、非法 JSON、空结果、重复请求、存储失败、恢复竞态和 Redis 失败写集成测试。

阶段 D：四工具去 Mock 和统一作品
- 绘画：真实文生图/图生图；上传文件签名、MIME、尺寸和像素校验；服务端创建 IMAGE 作品；历史只读作品 API；存储失败可重试且不重复扣点。
- 音乐：正确处理同步/异步任务；供应商音频拉取后存入私有存储；创建 MUSIC 作品；历史、收藏、下载、删除和版本链全部落库。`music_query` 是原任务受权子资源，不是独立工具。
- 编程：`ReferenceProgramming` 调用真实站内 code 契约；删除固定迷宫结果和伪控制台；HTML 严格 sandbox/CSP；Python/C++ 只生成、静态检查、下载；Scratch 无执行器时不声称执行；个人账号保存 CODE 作品。
- 阅读：服务端真实解析 TXT/MD/DOCX，DOC 在 LibreOffice 就绪前返回真实不可用状态，不假成功；执行真实清洗、分段、结构、关键词、问题和证据定位；回答携带当前文章/段落上下文；绘本图与段落关联；保存 READING JSON 作品；遵循 50 MiB/50,000 字/中文/1–6 年级/30 天规则。
- 作品中心：个人用户以云端作品为唯一真相源。只提供一次明确、用户确认的旧 `localStorage` 迁移；成功后清理对应旧记录，失败可重试且不重复。
- 新增统一浏览器临时存储适配层：学校强制 12 小时 `sessionStorage`；匿名只内存/当次 session；个人云端+明确草稿缓存。不使用 monkey-patch `window.localStorage` 作为主实现。

阶段 E：认证、学习、后台和数据闭环
- 使用自动生成并最终清理的临时数据，验证匿名、社会注册、培训邀请码注册、个人登录、学校登录、管理员登录、退出、停用、会话过期和越权拒绝。
- 后台 layout 服务端阻断未授权访问；登录模式和服务端账号类型语义一致，不只改文案。
- 验证课程分类→课程→模块→课时→公告→工具绑定→报名→学习→进度→课程作品完整闭环。
- 修正 `chat` 课程绑定放宽和阅读 vision/image 工具键不一致；所有上下文由服务端重新校验。
- 验证后台学校、培训、邀请码、套餐、人工订单、点数、AI 设置、内容、媒体、页面区块、课程、课件和审计。
- 不使用真实学生资料。测试结束后清理临时账号、业务数据、本地对象和 OSS 测试前缀，审计记录按策略保留。

阶段 F：部署就绪与生产验收（服务器资源准备后执行）
- 完成 Dockerfile/Compose 生产化：锁定 Node 支持版本，使用 `npm ci`，正确生成 Prisma Client，添加 health/readiness，明确迁移流程，Secret 路径与 Compose 一致。
- 实现与验证 ECS RAM Role 临时凭据，不回退到长期 AK/SK；对象键统一使用已确认 `krt-ai/*` 前缀。
- 生产使用 RDS PostgreSQL、ECS 自建 Redis 7、私有 OSS；数据库运行账号和迁移账号分离；运行账号做最小权限验证。
- 配置独立受控 Worker 处理 LibreOffice/FFmpeg，不让长转换占用 Web 请求主链路。
- 配置课件/媒体 Worker、`npm run ai:recover`、`npm run works:retention`定时任务，并验证重试和安全 Token。
- 验证 Nginx 80/443、HTTP→HTTPS、应用仅本机 3000、RDS/Redis 无公网端口、DigiCert 证书链与续期。
- 用合成授权的 DOC/DOCX/PPT/PPTX/PDF/图片/MP4/WebM/WebVTT 验证私有课件、水印、视频转码、封面、字幕、下线和越权拒绝。
- 完成匿名、个人、培训、学校、管理员五类身份真实测试，并完成 30/60 并发、成本、失败恢复、全局停用、RDS 备份恢复、OSS 生命周期和监控告警触发验证。
- 未完成上述真实环境验证前，不得使用“全功能生产可用”结论。

每完成一个阶段必须：
1. 更新 D:\programe\AI科瑞特\AI-create\长期记忆\科瑞特AI生产化前后端对接与全功能验收基线.md 的真实状态和验证证据。
2. 运行与改动相关的单元、集成、E2E、lint、凭据扫描和生产 build，修复回归后再继续。
3. 汇报具体改动、验证命令/结果、剩余风险和真实外部阻塞。
4. 不把“代码存在”、“页面返回 200”、“单元测试通过”写成角色端到端已验证。
5. 在安全且不包含 Secret/临时产物的前提下，按阶段整理 Git commit 并推送；但不盲目把工作区中无关的用户文件全部纳入提交。

最终本地交付必须包含：
- 不含 Mock 主链路的可运行代码。
- 统一前后端 AI 契约、类型和运行时校验。
- 数据库迁移和旧 `localStorage` 一次性迁移策略。
- 角色 E2E、API 集成测试、单元测试、路由/API 机器清单。
- 合成验收素材与可重复创建/清理的测试数据。
- 生产配置、RAM Role、Worker、备份、监控、灰度、回滚和后续服务器验收说明。
- 逐页面、逐功能验收报告，附命令结果；将未执行的生产验收仅列为外部阻塞，不伪造通过证据。

第一条工作回复应简要说明：已读取阶段 0 确认记录，将保留视觉基线，会先以子 agent 并行执行当前基线复核和独立工作，然后立即运行阶段 A 质量门禁。除非发现新的安全/费用/产品歧义，不要再停下询问已经在文档中确认的问题。
```
