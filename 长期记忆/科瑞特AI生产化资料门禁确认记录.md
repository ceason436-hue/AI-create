# 科瑞特 AI 生产化资料门禁确认记录

> 状态：`ACTIVE / 阶段 0 已通过`  
> 确认日期：2026-09-06  
> 开工口令：项目负责人已明确发送“资料已齐，可以开工”  
> 安全说明：本文不记录密码、完整连接串、AccessKey、API Key、Worker Token、SSH 私钥或真实学生资料。

## 1. 文档用途

本文固化生产化开工前已确认的资源、业务规则、安全边界、验收范围和延期项。实施和验收状态仍以《科瑞特AI生产化前后端对接与全功能验收基线》为主；本文中的“已提供”不等于功能已经通过端到端验收。

## 2. 工作区与交付权限

| 项目 | 状态 | 确认内容 |
|---|---|---|
| 最新视觉整改 | 已收到 | 当前大量未提交的视觉、响应式、动效及 `reference-*` 组件是必须保留的工作基线，生产化对接不得使视觉回退。 |
| 历史文件删除 | 已收到 | 旧部署文档、`project.tar.gz` 及已标记删除的旧文档是有意删除，不得恢复。 |
| Git 权限 | 已收到 | 允许创建 `codex/*` 分支、提交并推送到正式远端；每阶段提交前必须完成相关质量门禁。 |
| 当前预发布 | 已收到 | 使用本机作为当前预发布与自动化验收环境。 |
| 生产发布 | 延期 | 先完成本地代码、集成测试和可在本机完成的功能验收；RDS/OSS/ECS/Worker 真实接入及 30/60 并发在服务器准备完成后继续，延期项不得标记为已完成。 |

## 3. RDS PostgreSQL

| 字段 | 已确认值 / 状态 |
|---|---|
| 实例 ID | `pgm-bp1b5yb03b6i7786` |
| 引擎 | PostgreSQL 16.0 |
| 地域 / 可用区 | 华东 1（杭州）/ 杭州可用区 H |
| VPC | `vpc-bp126y47t163885pd3stc` |
| RDS vSwitch | `vsw-bp1xlpqwa22lofewy5aao` |
| ECS vSwitch | `vsw-bp1bijnko5pcf69mmk7uf` |
| 数据库 | `krt_ai`，已创建，当前为空库 |
| 字符集 / Collate / Ctype | `UTF8` / `C` / `en_US.utf8` |
| 连接 | 专有网络内网地址，端口 `5432` |
| `DATABASE_URL` | 已写入 `.env.production.local`；已脱敏验证为可解析 PostgreSQL URL，目标数据库、端口和 SSL 参数正确 |
| 迁移账号 | `krt_migrator`，标准账号，已创建并获得 `krt_ai` 读写权限 |
| 运行账号 | `krt_runtime`，标准账号，已创建并获得 `krt_ai` 读写权限 |
| 高权限管理账号 | 已创建，不作为应用常驻账号 |
| SSL | 已开启，CA 证书可下载 |
| 白名单 | 当前 `172.16.0.0/12`，包含 ECS 私网 IP `172.25.249.245` |
| 白名单加固 | 生产前待办：收紧至 ECS 私网 IP `/32` 或使用 ECS 安全组授权 |
| 备份 | 每日 05:00–06:00；快照保留 14 天；日志备份开启并保留 14 天；秒级备份开启 |
| 实例释放保护 | 保留最后一份备份 |
| 跨地域备份 | 当前关闭，记为可接受的首发风险，不宣称具备异地灾备 |
| 恢复目标 | RPO 15 分钟，RTO 4 小时；上线前完成一次隔离恢复演练，后续每季度一次 |

RDS 与 ECS 在同一 VPC 中，可用区和 vSwitch 不同不阻止 VPC 内网访问。本机不强行连接 RDS 内网地址，实际连通性、Prisma 迁移和最小权限验证延后到 ECS/生产基础设施阶段。

## 4. Redis

| 项目 | 状态 / 决策 |
|---|---|
| 方案 | ECS 自建 Redis 7，不购买云 Redis |
| 本地环境 | 已验证 Redis 7 容器 healthy，认证后 `PONG` |
| 生产 `REDIS_URL` | 延期，当前 `.env.production.local` 保持为空 |
| 生产要求 | 与应用使用共享 Docker 网络；密码认证；AOF；不开放公网 6379；限流组件故障时 AI 请求安全拒绝 |

## 5. OSS

| 字段 | 已确认值 / 状态 |
|---|---|
| Bucket | `krt-ai-works-hz-a7m2q9x4` |
| 地域 | 华东 1（杭州） |
| 内网 Endpoint | `oss-cn-hangzhou-internal.aliyuncs.com` |
| 外网 Endpoint | `oss-cn-hangzhou.aliyuncs.com` |
| ACL | Private |
| 阻止公共访问 | 已开启 |
| 存储类型 | 标准存储，同城冗余 ZRS |
| 加密 | OSS 托管 AES256 |
| 版本控制 | 已开启 |
| 前缀 | 采用 `krt-ai/works/`、`krt-ai/courseware/original/`、`krt-ai/courseware/preview/`、`krt-ai/media/original/`、`krt-ai/media/processed/`、`krt-ai/test/`、`krt-ai/temp/` |
| 生命周期 | 已配置；测试与临时前缀自动清理；正式作品、课件和媒体不使用全局固定删除规则 |
| CORS | 首发优先服务端代理；不使用 `AllowedOrigin=*` |
| 访问方式 | 已选择 ECS RAM Role，角色创建、最小权限策略和实例绑定延期到正式 ECS 阶段 |
| 生产 Secret | `OSS_BUCKET`、`OSS_ENDPOINT` 已写入；长期 AK/SK 保持为空 |

RAM Role 最终只允许指定 Bucket 与 `krt-ai/*` 对象的必要 List/Get/Put/Delete/分片操作，不允许修改 Bucket ACL、版本或生命周期，不允许访问其他 Bucket。

## 6. ECS、Nginx 与 TLS

| 字段 | 已确认值 / 状态 |
|---|---|
| 当前 ECS | 2 核 2GB，华东 1（杭州），Ubuntu 24.04 64 位 |
| VPC | `vpc-bp126y47t163885pd3stc` |
| vSwitch | `vsw-bp1bijnko5pcf69mmk7uf` |
| 私网 IP | `172.25.249.245` |
| Nginx | 已安装 |
| 域名 | `https://lingpeak.com` |
| TLS 证书 | DigiCert RSA 2048，到期时间 2027-03-05 00:18:37 |
| 容量结论 | 2C2G 可用于轻量预发布，不能作为 Next.js + Redis + LibreOffice + FFmpeg + 30/60 并发的最终验收环境 |
| 升级决策 | 当前不升级；本地功能通过后再配置正式服务器，最终生产建议至少 4C8G 或独立 Worker |
| SSH | 延期；后续只提供主机、端口、用户、sudo 能力与本机私钥路径/别名，不通过对话传输私钥或密码 |

生产网络目标：公网只开放 80/443；SSH 只允许受信来源；Next.js 3000 仅供 Nginx/本机代理；PostgreSQL 5432 和 Redis 6379 不开放公网；HTTP 跳转 HTTPS；Nginx 反代到 `127.0.0.1:3000`。

## 7. Worker 与定时任务

| 项目 | 已确认决策 |
|---|---|
| LibreOffice | 允许安装，最终路径由部署环境实测 |
| FFmpeg | 允许安装，版本与路径按 Ubuntu 24.04/容器环境实测 |
| Worker Token | 延期到正式 ECS 阶段，只写 Secret |
| 课件 Worker | 建议每分钟处理队列 |
| 媒体 Worker | 建议每分钟处理队列 |
| AI 异常恢复 | `npm run ai:recover`，每 5 分钟 |
| 作品保留期 | `npm run works:retention`，每日 02:30，`Asia/Shanghai` |

## 8. MiniMax、预算与点数

| 字段 | 已确认值 / 规则 |
|---|---|
| 账号地域 | 中国 |
| Base URL | `https://api.minimaxi.com` |
| Group ID | 不适用，控制台无该字段 |
| API Key | 已写入 Secret，本文不记录 |
| 模型名称 | 原网站模型名作为待验证候选，不直接视为当前官方契约 |
| 官方契约 | 图生图字段、音乐同步/异步返回、模型 ID、请求限制、成功字段与错误码必须经当前官方文档和真实脱敏预发布响应验证 |
| 生产开关 | `AI_GENERATION_ENABLED=false` 已验证 |
| 测试总预算 | 人民币 100 元 |
| 测试单日上限 | 人民币 30 元 |
| 自动停止线 | 达到预算 80% 时停止自动测试并汇报 |
| Token 额外上限 | 一亿 Token；仅作为文本类附加限制，不代替人民币预算 |
| 点数换算 | `1 点 = 0.01 元供应商成本`；各工具正式权重按单次实测成本 × 1.3 安全系数后向上取整 |

未完成成本和失败恢复验收前，不允许开启生产 AI 总开关。

## 9. 用户、作品和功具固定规则

| 项目 | 已确认规则 |
|---|---|
| 社会/培训个人账号 | 作品进入 PostgreSQL + 私有 OSS，可跨设备查看、下载和删除 |
| 学校共享账号 | 仅使用 12 小时 `sessionStorage`，不创建云端作品、不上传 OSS |
| 匿名试用 | 不写个人作品；仅保留不含作品内容的脱敏用量审计 |
| 学校工具 | 后台逐校配置，默认全部关闭；必须由服务端强制 |
| 社会注册 | 首发开放用户名 + 密码注册 |
| 培训注册 | 个人账号 + 邀请码，作品云端保存 |
| 短信、找回密码 | 本轮不适用；前台不得宣称已支持 |
| 在线支付 | 本轮不适用；采用管理员人工开通套餐 |
| 音乐收藏 | 必须落库为长期状态，不继续仅使用本地存储 |
| 音乐继续编辑 | 必须保存版本链 |
| 编程运行 | HTML 使用严格 sandbox iframe；Python/C++ 只生成、静态检查和下载，没有真实执行器时不声称运行成功；Scratch 没有真实执行器时不声称执行 |

## 10. 课程 URL 规则

| 地址 | 用途 |
|---|---|
| `/courses` | 课程总目录 |
| `/courses/directions/[slug]` | 静态课程方向/营销介绍 |
| `/courses/[courseSlug]` | 数据库真实可报名课程 |

旧静态方向地址使用 308 永久重定向迁移到 `/courses/directions/[slug]`，数据库课程 Slug 保持简洁、稳定且唯一。

## 11. AI 阅读限制

| 字段 | 已确认值 |
|---|---|
| 上传文件最大 | 50 MiB |
| 提取正文最大 | 50,000 个中文字符 |
| 语言 | 中文 |
| 年级 | 一至六年级 |
| 超长处理 | 分块分析，服务端合并结构化结果 |
| 保留期 | 正文、分析结果和阅读作品保留 30 天 |

文件上传必须校验真实签名、MIME、扩展名、解压后大小和文本长度，特别防止 DOCX 等 ZIP 容器格式的压缩炸弹。

## 12. 测试身份与素材

| 项目 | 已确认决策 |
|---|---|
| 临时身份 | 授权自动创建并最终清理临时管理员、个人、培训、学校账号 |
| 临时业务数据 | 授权创建并清理课程、模块、课时、公告、报名、咨询、课件、媒体、作品与 OSS 测试对象 |
| 审计日志 | 按策略保留，不为让测试“干净”而删除审计证据 |
| 验收素材 | 由实施阶段自动生成并明确标记为“自动化验收合成素材”，不使用真实学生资料 |
| 格式 | TXT、MD、DOC、DOCX、PPT、PPTX、PDF、JPG、PNG、WebP、MP4、WebM、WebVTT 以及课程/CMS/阅读/图生图测试内容 |
| 手册素材授权 | 《AI科瑞特手册》中的真实教师、证书、奖项、学校 Logo 和课堂照片已确认拥有正式公开授权 |

## 13. 合规边界

本轮只落实已确认的最低安全机制，包括服务端身份与权限、私有存储、最小数据、日志脱敏、限流、并发、审计与内容错误状态。本轮不代表已完成未成年人保护、隐私、内容安全、算法或生成式 AI 相关的完整法律合规认证，正式对学生开放前仍需专项复核。

## 14. 监控与告警

| 项目 | 状态 / 决策 |
|---|---|
| 告警联系人组 | `KRT-AI-OPS` 已创建 |
| 告警接收人 | 项目负责人 |
| 技术值守 | 暂由项目负责人承担 |
| 待配置指标 | ECS CPU/内存/磁盘、RDS CPU/连接/存储、Redis 连通、网站可用性、HTTP 5xx、AI 失败率与成本、Worker 积压、OSS 失败、超时 AI 预占 |
| 配置时机 | 资源级规则可在服务器阶段配置；应用级告警在指标和部署链路完成后配置并做触发验证 |

## 15. 环境文件脱敏验证

2026-09-06 只读检查结果：

| 变量 | 状态 |
|---|---|
| `.env.production.local` | 已存在，已被 Git ignore |
| `DATABASE_URL` | 已写入，语法与目标参数脱敏验证通过 |
| `NEXT_PUBLIC_SITE_URL` | 已写入 |
| `MINIMAX_API_KEY` | 已写入 Secret |
| `MINIMAX_GROUP_ID` | 空，已确认不适用 |
| `MINIMAX_BASE_URL` | 已写入 |
| `STORAGE_DRIVER` | `OSS` |
| `OSS_BUCKET` / `OSS_ENDPOINT` | 已写入并与确认资源匹配 |
| OSS AK/SK/STS | 空，等待 ECS RAM Role 适配 |
| `AI_GENERATION_ENABLED` | `false` |
| `REDIS_URL` | 空，已明确延期 |
| Worker Token / `SOFFICE_PATH` / `FFMPEG_PATH` | 空，已明确延期 |
| 一次性管理变量 | 正常运行时保持为空 |

## 16. 阶段 0 结论与实施约束

资料收集门禁已通过，可进入阶段 A。开工口令只授权按本文与主基线已确认的范围实施，不自动授权：

- 发布到正式生产环境或公开新内容；
- 删除生产数据、真实用户、备份或正式 OSS 对象；
- 超过已确认 AI 预算；
- 创建或扩容付费云资源；
- 向外部人员发送消息、账号或资料。

后续发现会影响安全、费用、生产数据、产品行为或公开发布的新歧义时，暂停相关分支并由总技术负责人集中请项目负责人确认；其他不依赖的已确认工作可继续。
