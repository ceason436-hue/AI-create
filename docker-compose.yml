version: '3.8'

services:
  next-app:
    build:
      context: ./ai-music-app
      dockerfile: Dockerfile
    container_name: ai-music-app
    ports:
      - "80:3000"
    restart: always
    environment:
      - NODE_ENV=production
